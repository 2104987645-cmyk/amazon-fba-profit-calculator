(function(r,f){const a=f();if(typeof module==='object'&&module.exports)module.exports=a;r.KnowledgeAnswerPlanner=a;})(typeof window!=='undefined'?window:globalThis,function(){'use strict';function plan(q,claims,bundles){const bs=bundles||[],ids=s=>bs.filter(b=>b.sufficiency&&b.sufficiency.status===s).map(b=>b.claimId),conf=ids('conflicted'),ins=ids('insufficient'),part=ids('partially-supported'),byId=new Map((claims||[]).map(c=>[c.claimId,c]));const account=bs.some(b=>{const c=byId.get(b.claimId)||{};return(c.claimType==='account-fact'||c.intent==='account-specific')&&b.sufficiency&&b.sufficiency.status!=='sufficient';});const type=account?'account-data-required':conf.length?'conflicted':ins.length?'insufficient':part.length?'qualified':'direct';return{planId:'answer-'+((q&&q.normalizedQuestion)||''),question:(q&&q.question)||'',answerType:type,primaryClaimIds:ids('sufficient'),supportingClaimIds:part,conflictedClaimIds:conf,insufficientClaimIds:ins,sections:[],includeLimitations:true,includeSources:true,includeOperationalAdvice:true,includeAccountFindings:true,metadata:{scope:(q&&q.scope)||{}}};}return{plan};});
