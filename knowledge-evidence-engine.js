(function(root,factory){const api=factory(typeof module==='object'&&module.exports?require('./knowledge-evidence-candidate-builder'):root.KnowledgeEvidenceCandidateBuilder,typeof module==='object'&&module.exports?require('./knowledge-claim-verifier'):root.KnowledgeClaimVerifier,typeof module==='object'&&module.exports?require('./knowledge-evidence-policy'):root.KnowledgeEvidencePolicy,typeof module==='object'&&module.exports?require('./knowledge-evidence-conflict'):root.KnowledgeEvidenceConflict,typeof module==='object'&&module.exports?require('./knowledge-evidence-sufficiency'):root.KnowledgeEvidenceSufficiency);if(typeof module==='object'&&module.exports)module.exports=api;root.KnowledgeEvidenceEngine=api;})(typeof window!=='undefined'?window:globalThis,function(C,V,P,F,S){'use strict';async function buildBundle(claim,items,ctx){const candidates=C.buildCandidates(claim,items),verifier=V.createVerifier({verifier:ctx&&ctx.claimVerifier}),verifications=await verifier.verifyBatch(claim,candidates,ctx),assessments=candidates.map((x,i)=>P.evaluate(x,claim,verifications[i],ctx)),conflicts=F.detect(assessments),sufficiency=S.evaluate(claim,assessments,conflicts);return{claimId:claim.claimId,candidates,verifications,assessments,conflicts,sufficiency,diagnostics:{candidateCount:candidates.length,supportCount:assessments.filter(x=>x.supportStatus==='supports').length,contradictionCount:assessments.filter(x=>x.supportStatus==='contradicts').length}};}return{buildBundle};});
