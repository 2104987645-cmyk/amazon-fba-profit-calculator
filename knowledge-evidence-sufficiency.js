(function(root,factory){const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;root.KnowledgeEvidenceSufficiency=api;})(typeof window!=='undefined'?window:globalThis,function(){'use strict';function evaluate(claim,assessments,conflicts){if((conflicts||[]).some(x=>!['none','unknown'].includes(x.type)))return{status:'conflicted',missingRequirements:[]};const good=(assessments||[]).filter(a=>a.admissible&&a.supportStrength==='direct'&&a.authorityStatus==='meets'&&a.confidence!=='insufficient'),need=(claim.authorityRequirement&&claim.authorityRequirement.mustInclude)||[];if(!good.length)return{status:'insufficient',missingRequirements:need};return good.some(a=>a.confidence==='high')?{status:'sufficient',missingRequirements:[]}:{status:'partially-supported',missingRequirements:[]};}return{evaluate};});
