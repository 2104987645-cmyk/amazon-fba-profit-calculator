'use strict';
const assert=require('node:assert/strict'),C=require('../knowledge-answer-citations');
function b(role='supporting',admissible=true,candidate={}){const c={candidateId:'candidate-a',retrievalItemId:'retrieval-a',sourceId:'government',providerId:'gov',externalId:'ext',title:'Title',url:'https://same.example',publisher:'Pub',author:'Author',publishedAt:'2026-01-01',effectiveAt:'2026-02-01',retrievedAt:'2026-03-01',metadata:{nested:true},...candidate};return{claimId:'claim-c',sufficiency:{status:'sufficient'},candidates:[c],assessments:[{assessmentId:'assessment-a',candidateId:c.candidateId,admissible,evidenceRole:role}]};}
const input=[b()],before=JSON.stringify(input),citations=C.planCitations(input),cite=citations[0];assert.equal(JSON.stringify(input),before);assert.deepEqual(cite,{citationId:'cite-retrieval-a',sourceId:'government',providerId:'gov',externalId:'ext',title:'Title',url:'https://same.example',publisher:'Pub',author:'Author',publishedAt:'2026-01-01',effectiveAt:'2026-02-01',retrievedAt:'2026-03-01',retrievalItemId:'retrieval-a',candidateIds:['candidate-a'],assessmentIds:['assessment-a'],claimIds:['claim-c'],usageRole:'factual-support',metadata:{nested:true}});assert.notEqual(cite.candidateIds[0],cite.retrievalItemId);
assert.equal(C.planCitations([b('primary',true)])[0].usageRole,'factual-support');assert.equal(C.planCitations([b('supporting',true)])[0].usageRole,'factual-support');assert.equal(C.planCitations([b('contextual',true)])[0].usageRole,'context');assert.equal(C.planCitations([b('conflicting',false)])[0].usageRole,'conflict');assert.equal(C.planCitations([b('inadmissible',false)]).length,0);assert.equal(C.planCitations([b('supporting',false)]).length,0);
const missing=C.planCitations([b('supporting',true,{url:null,publisher:null,author:null,publishedAt:null,effectiveAt:null})])[0];assert.equal(missing.url,null);assert.equal(missing.publisher,null);assert.equal(missing.author,null);assert.equal(missing.publishedAt,null);assert.equal(missing.effectiveAt,null);
console.log('citations passed');
