'use strict';const assert=require('node:assert/strict'),E=require('../knowledge-evidence-engine');const claim={claimId:'vine-us',text:'Vine eligibility',claimType:'eligibility',topic:'brand-reviews',intent:'eligibility',marketplaces:['US'],regions:[],requestedYear:'2026',freshness:'current',authorityRequirement:{mustInclude:['amazon-official']},temporalRequirement:{mode:'current',maxAgeDays:30},metadata:{}};const doc={id:'d',requestId:'r',providerId:'amazon-seller-help-provider',sourceId:'amazon-seller-help',sourceType:'documentation',authorityClass:'amazon-official',url:'https://sellercentral.amazon.com/x',externalId:'x',marketplaces:['US'],regions:[],content:'untrusted text',publishedAt:'2026-01-01',metadata:{}};async function run(){const ok=await E.buildBundle(claim,[doc],{now:'2026-01-02',claimVerifier:{verify(){return{status:'supports',supportStrength:'direct'};}}});assert.equal(ok.sufficiency.status,'sufficient');const irrelevant=await E.buildBundle(claim,[doc],{now:'2026-01-02',claimVerifier:{verify(){return{status:'not-addressed',supportStrength:'none'};}}});assert.equal(irrelevant.sufficiency.status,'insufficient');const fake=await E.buildBundle(claim,[{...doc,id:'c',sourceId:'community',authorityClass:'community'}],{now:'2026-01-02',claimVerifier:{verify(){return{status:'supports',supportStrength:'direct'};}}});assert.equal(fake.sufficiency.status,'insufficient');}run().then(()=>console.log('evidence integration passed'));
