'use strict';const assert=require('node:assert/strict'),B=require('../knowledge-evidence-candidate-builder');const c={claimId:'c'},i={id:'i',requestId:'r',providerId:'p',sourceId:'amazon-seller-help',sourceType:'documentation',authorityClass:'amazon-official',url:'https://x',externalId:'x',marketplaces:['US'],regions:[],metadata:{}};const x=B.buildCandidate(c,i,0);assert.equal(x.claimId,'c');assert.equal(x.retrievalItemId,'i');assert.equal(x.contentAcquisition,'snippet');assert.equal(x.confidence,undefined);assert.equal(B.buildCandidate(c,{id:'bad'},1),null);console.log('candidate builder passed');
