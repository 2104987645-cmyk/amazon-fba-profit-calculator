'use strict';const assert=require('node:assert/strict'),F=require('../knowledge-evidence-conflict');assert.equal(F.detect([])[0].type,'none');assert.equal(F.detect([{supportStatus:'supports'}])[0].type,'unknown');const x=F.detect([{assessmentId:'a',supportStatus:'supports',authorityStatus:'meets',freshnessStatus:'fresh',scopeStatus:'match'},{assessmentId:'b',supportStatus:'contradicts',authorityStatus:'meets',freshnessStatus:'stale',scopeStatus:'match'}]);assert.ok(['support-vs-contradiction','authority-conflict','temporal-conflict','version-conflict'].every(t=>x.some(a=>a.type===t)));console.log('evidence conflict passed');
