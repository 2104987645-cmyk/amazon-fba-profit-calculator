'use strict';const assert=require('node:assert/strict'),G=require('../knowledge-answer-grounding');const b={claimId:'c',candidates:[{candidateId:'candidate-a',retrievalItemId:'retrieval-a',providerId:'p',sourceId:'s'}],assessments:[{admissible:true,evidenceRole:'supporting',assessmentId:'a',claimId:'c',candidateId:'candidate-a'}]};const before=JSON.stringify(b),refs=G.buildGroundingReferences(b);assert.equal(JSON.stringify(b),before);assert.equal(refs.length,1);assert.equal(refs[0].retrievalItemId,'retrieval-a');assert.equal(G.buildGroundingReferences({assessments:[{candidateId:'missing'}]}).length,0);console.log('grounding passed');
