'use strict';const assert=require('node:assert/strict'),P=require('../knowledge-answer-planner');assert.equal(P.plan({},[],[{claimId:'c',sufficiency:{status:'sufficient'}}]).answerType,'direct');assert.equal(P.plan({question:'q'},[{claimId:'a',claimType:'account-fact'}],[{claimId:'a',sufficiency:{status:'insufficient'}}]).answerType,'account-data-required');assert.equal(P.plan({question:'q'},[],[{claimId:'p',sufficiency:{status:'partially-supported'}}]).answerType,'qualified');console.log('planner passed');
