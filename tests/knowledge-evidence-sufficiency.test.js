'use strict';const assert=require('node:assert/strict'),S=require('../knowledge-evidence-sufficiency');const c={authorityRequirement:{mustInclude:['amazon-official']}};const good={admissible:true,supportStrength:'direct',authorityStatus:'meets',confidence:'high'};assert.equal(S.evaluate(c,[good],[]).status,'sufficient');assert.equal(S.evaluate(c,[{...good,confidence:'medium'}],[]).status,'partially-supported');assert.equal(S.evaluate(c,[],[]).status,'insufficient');assert.equal(S.evaluate(c,[good],[{type:'support-vs-contradiction'}]).status,'conflicted');console.log('evidence sufficiency passed');
