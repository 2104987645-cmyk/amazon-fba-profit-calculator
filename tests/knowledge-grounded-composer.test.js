'use strict';const assert=require('node:assert/strict'),C=require('../knowledge-grounded-composer');const claims=new Map([['c',{text:'x'}]]),g=new Map([['c',[{admissible:true,evidenceRole:'supporting'}]]]);assert.equal(C.compose({},[{claimId:'c',sufficiency:{status:'sufficient'}}],claims,g).officialFacts.length,1);assert.equal(C.compose({},[{claimId:'c',sufficiency:{status:'sufficient'}}],new Map(),g).missingClaimTextIds[0],'c');console.log('composer passed');
