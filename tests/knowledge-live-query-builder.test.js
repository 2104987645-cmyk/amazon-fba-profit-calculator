'use strict';const assert=require('node:assert/strict');const B=require('../knowledge-live-query-builder'),A=require('../knowledge-source-access-registry');const r={requestId:'r',providerId:'amazon-seller-help-provider',sourceId:'amazon-seller-help',query:{normalizedQuestion:'why B0ABCDEFGHI order 123-1234567-1234567',intent:'account-specific',topic:'listing',marketplaces:['US','CA'],regions:['EU','GLOBAL'],requestedYear:'2026',freshness:'current'},constraints:{requiresFreshVerification:true,maxAgeDays:14},jurisdictions:['US','CA','EU']};const q=B.buildSearchRequest(r,A.getPolicy(r.sourceId),{maxResults:20});assert.ok(q.query.includes('Seller Help'));assert.equal(/B0ABCDEFGHI|123-1234567/.test(q.query),false);assert.deepEqual(q.domains,['sellercentral.amazon.com']);assert.equal(q.recencyDays,14);assert.equal(q.maxResults,10);assert.equal(q.metadata.requestedYear,'2026');assert.deepEqual(q.metadata.marketplaces,['US','CA']);assert.deepEqual(q.metadata.regions,['EU','GLOBAL']);assert.equal(B.buildSearchRequest({...r,query:{...r.query,freshness:'historical'},constraints:{requiresFreshVerification:false,maxAgeDays:14}},A.getPolicy(r.sourceId)).recencyDays,null);assert.ok(B.buildQueryText({...r,sourceId:'amazon-ads'}).includes('Amazon Ads'));console.log('live query builder passed');
