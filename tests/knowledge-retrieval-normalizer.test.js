'use strict';const assert=require('node:assert/strict');const N=require('../knowledge-retrieval-normalizer');const req={requestId:'r',providerId:'amazon-seller-help-provider',sourceId:'amazon-seller-help'};const raw={externalId:'x',title:'Same title',url:' https://a.example/x?q=1#part ',authorityClass:'regulator',marketplaces:['US','US',''],regions:['EU'],publishedAt:null,effectiveAt:null,retrievedAt:'2026-01-01',metadata:{a:1},claim:'ignored',confidence:'bad'};const before=JSON.stringify(raw);const item=N.normalizeItem(req,raw,0);assert.equal(item.sourceType,'documentation');assert.equal(item.authorityClass,'amazon-official');assert.equal(item.url,'https://a.example/x?q=1');assert.deepEqual(item.marketplaces,['US']);assert.equal(item.claim,undefined);assert.equal(item.confidence,undefined);assert.equal(JSON.stringify(raw),before);const community=N.normalizeItem({...req,providerId:'community-provider',sourceId:'community'},{externalId:'c',title:'c',authorityClass:'amazon-official'},0);assert.equal(community.authorityClass,'community');assert.equal(N.normalizeItem(req,{title:'bad'},0),null);assert.equal(N.normalizeItem({...req,providerId:'amazon-official-forum-provider',sourceId:'amazon-official-forum'},{externalId:'f',title:'f',officialAuthorVerified:true},0).officialAuthorVerified,true);assert.equal(N.normalizeItem({...req,providerId:'user-account-provider',sourceId:'user-account'},{externalId:'a',title:'a',url:null},0).externalId,'a');const first={normalizedItems:[item]},sameExternal={normalizedItems:[{...item,id:'two',url:'https://b',externalId:'x'}]},sameUrl={normalizedItems:[{...item,id:'three',externalId:'z'}]},similar={normalizedItems:[{...item,id:'four',externalId:'q',url:'https://q',title:'Same title'}]};assert.deepEqual(N.normalizeBatch([first,sameExternal,sameUrl,similar]).map(x=>x.id),[item.id,'four']);console.log('knowledge retrieval normalizer tests passed');
