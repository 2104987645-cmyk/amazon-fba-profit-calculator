'use strict';const assert=require('node:assert/strict');const R=require('../knowledge-source-access-registry');const x=R.listPolicies();assert.equal(x.length,8);assert.equal(new Set(x.map(a=>a.sourceId)).size,8);assert.equal(R.getPolicy('amazon-seller-help').providerId,'amazon-seller-help-provider');assert.ok(R.getPolicy('amazon-seller-help').allowedHosts.includes('sellercentral.amazon.com'));assert.ok(R.getPolicy('amazon-seller-university').allowedHosts.includes('sell.amazon.com'));assert.ok(R.getPolicy('amazon-ads').allowedHosts.includes('advertising.amazon.com'));assert.ok(R.getPolicy('amazon-sp-api').allowedHosts.includes('developer-docs.amazon.com'));assert.equal(R.getPolicy('amazon-official-forum').requiresAuthorVerification,true);assert.equal(R.getPolicy('government').requiresGovernmentAuthorityRegistry,true);assert.ok(R.getPolicy('community').allowedHosts.includes('reddit.com'));assert.equal(R.getPolicy('community').requiresHttps,true);const p=R.getPolicy('community');p.allowedHosts.push('evil');assert.equal(R.getPolicy('community').allowedHosts.includes('evil'),false);assert.equal(R.getPolicy('missing'),null);assert.equal(R.validatePolicies().valid,true);console.log('source access registry passed');
