(function(root,factory){const api=factory(typeof module==='object'&&module.exports?require('./knowledge-source-registry'):root.KnowledgeSourceRegistry);if(typeof module==='object'&&module.exports)module.exports=api;root.KnowledgeRetrievalProviderRegistry=api;})(typeof window!=='undefined'?window:globalThis,function(SourceRegistry){'use strict';
const CHANNELS=Object.freeze(['public-web','internal-index','account-data']);
const definitions=Object.freeze([
['amazon-seller-help','Amazon Seller Help','public-web'],['amazon-seller-university','Amazon Seller University','public-web'],['amazon-seller-news','Amazon Seller News','public-web'],['amazon-ads','Amazon Ads','public-web'],['amazon-sp-api','Amazon SP-API','public-web'],['amazon-official-forum','Amazon Official Forum','public-web'],['government','Government / Regulator','public-web'],['internal-knowledge','Internal Knowledge','internal-index'],['community','Community','public-web'],['user-account','User Account','account-data']
].map(([sourceId,label,channel])=>Object.freeze({id:sourceId+'-provider',label,sourceIds:[sourceId],channel,adapterKey:sourceId+'-provider',resultKind:sourceId==='user-account'?'account-record':'document',requiresAdapter:true,requiresAccountConnection:sourceId==='user-account',requiresOfficialAuthorVerification:sourceId==='amazon-official-forum',supportsFreshRetrieval:sourceId!=='internal-knowledge',supportsHistoricalRetrieval:sourceId!=='user-account',notes:'Phase 7.2 descriptor only; adapter is injected at runtime.'})));
function clone(x){return JSON.parse(JSON.stringify(x));}function listProviders(){return definitions.map(clone);}function getProvider(id){const p=definitions.find(x=>x.id===id);return p?clone(p):null;}function hasProvider(id){return definitions.some(x=>x.id===id);}function getProviderForSource(sourceId){const p=definitions.find(x=>x.sourceIds.includes(sourceId));return p?clone(p):null;}function getProviderIdForSource(sourceId){const p=definitions.find(x=>x.sourceIds.includes(sourceId));return p?p.id:null;}function validateBindings(){const errors=[];const bound=[];definitions.forEach(p=>p.sourceIds.forEach(id=>{bound.push(id);if(!SourceRegistry.hasSource(id))errors.push('UNKNOWN_SOURCE:'+id);}));SourceRegistry.SOURCE_IDS.forEach(id=>{if(bound.filter(x=>x===id).length!==1)errors.push('BINDING_COUNT:'+id);});return{valid:errors.length===0,errors};}
return{PROVIDER_CHANNELS:CHANNELS.slice(),listProviders,getProvider,hasProvider,getProviderForSource,getProviderIdForSource,validateBindings};});
